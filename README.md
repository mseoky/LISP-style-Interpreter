# LISP-style-Interpreter
A LISP-style interpreter implemented in C, featuring a dynamically typed runtime with generic vectors, lists, and symbols. Supports extensible built-in operations, environment-based variable binding, and side-effecting vector operations such as indexing, insertion, deletion, slicing, sorting, and stack/queue semantics.
